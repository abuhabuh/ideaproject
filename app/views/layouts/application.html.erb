<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01//EN" "http://www.w3.org/TR/html4/strict.dtd">
<html>
  <head>
    <!-- title is function from app/helpers/ApplicationHelper -->
    <title><%= title %></title>
    <%= csrf_meta_tag %>
    
    <%= javascript_include_tag "application" %>
    
    <%= render 'layouts/stylesheets' %>
    <%= stylesheet_link_tag('facebox') %>    
    <%= yield :head %>
        
  </head>
  <body>
    <div class="wrapper">
      <%= render 'layouts/header' %>
        <div class="container" id="user_nav">
    	  <%= yield %>
        </div>
        <div class="push"></div> <!-- used to keep footer in place -->
    </div>
     <!-- %= render 'layouts/footer' %> Commented out for now cause footer doesn't work on home page when logged in -->
  </body>
  
</html>
