<h1>Listing deals</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Purchase Link</th>
    <th>Price</th>
    <th>Start date</th>
    <th>End date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @deals.each do |deal| %>
  <tr>
    <td><%= deal.title %></td>
    <td><%= deal.description %></td>
    <td><%= deal.purchase_link %></td>
    <td><%= deal.price %></td>
    <td><%= deal.start_date %></td>
    <td><%= deal.end_date %></td>
    <td><%= link_to 'Show', deal %></td>
    <td><%= link_to 'Edit', edit_deal_path(deal) %></td>
    <td><%= link_to 'Destroy', deal, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Deal', new_deal_path %>
