<%= form_for(@idea_commitment) do |f| %>
  <% if @idea_commitment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@idea_commitment.errors.count, "error") %> prohibited this idea_commitment from being saved:</h2>

      <ul>
      <% @idea_commitment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :idea %><br />
    <%= f.text_field :idea %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
