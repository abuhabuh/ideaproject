<%= form_for(@user_idea_time) do |f| %>
  <% if @user_idea_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_idea_time.errors.count, "error") %> prohibited this user_idea_time from being saved:</h2>

      <ul>
      <% @user_idea_time.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="field">
    <%= f.label :idea %><br />
    <%= f.text_field :idea %>
  </div>
  <div class="field">
    <%= f.label :time %><br />
    <%= f.datetime_select :time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
