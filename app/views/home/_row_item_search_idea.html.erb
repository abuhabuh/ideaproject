            <% idea_owner = User.find(idea_item.creator) %>
            <tr>
              <td><%= link_to image_tag(idea_owner.profile_pic.url(:stream)),
                              user_path(idea_owner.id),
                              {:method => 'get'} %></td>
              <td>
                <%= link_to idea_item.text,
                              idea_path(idea_item.id),
                              {:method => 'get'} %><br/>
                <% unless @user_idea_ids.include?(idea_item.id.to_s) %>
                  <%= render :partial => "home/idea_action_add",
                             :locals => {:idea_id => idea_item.id} %>
                <% end %>
              </td>
              <td>
                <div id="<%=ID_TAG_AUTH_HOME_STREAM_NUM_USERS+idea_item.id.to_s%>"><%=idea_item.num_users_joined%></div>
              </td>
            </tr>

