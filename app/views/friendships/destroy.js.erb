$(document).ready(function(){

    $("#<%=ID_TAG_USER_LIST_USER_ROW + @friend.id.to_s%>").replaceWith
        ("<%= escape_javascript (
            render :partial => "users/list_users", :locals => {:user => @friend}
            ) %>");
            
    $("#flash_notice_div").html("<%= escape_javascript (
        render :partial => "layouts/flash_notice", :locals => {:notice => USER_ACTION_FRIENDSHIP_REMOVED}
        )%>");
    
    <% flash.discard %>

});
